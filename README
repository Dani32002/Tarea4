Pregunta 1:
En las configuraciones de ejecución los argumentos de entrada son el algoritmo que se desea 
ejecutar (BellmanFord, Dijkstra o FloydWarshall) y el segundo argumento es el nombre del archivo (txt) que contiene
un grafo dirigido de n vertices con costos naturales. Debe ser una matriz con los costos de ir de un vertice a otro,
si no hay eje, se pone -1. Se deben presentar los numeros separados por tabulaciones y por filas.
Ej: Dijkstra distances5.txt
El retorno seria una matriz que representa los costos mínimos de cada vertice hacia todos los vértices del grafo
junto con el tiempo de ejecución.

Pregunta 2:
En las configuraciones de ejecución el argumento debe ser el nombre del archivo (txt) representando un grafo no dirigido
simetrico. Se deben presentar los numeros separados por tabulaciones y por filas.
Ej: distances5.txt
El retorno sería los componentes fuertemente conectados como una lista con las listas de vértices de los componentes
fuertemente conectados.

Pregunta 3:
En las configuraciones de ejecución el argumento debe ser el nombre del archivo (txt) con una matriz como la del problema 1.
Ej: distances5.txt
El retorno es un boolean que indica si hay un ciclo (True) o no (False) en el grafo.

